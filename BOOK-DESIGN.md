# 全书章节设计

本文说明现行章节排布的依据、内容边界和分册候选。实际标题与阅读顺序以 [全书目录](SUMMARY.md) 为唯一来源；写作方法见 [写作约定](WRITING.md)。本轮完成的是结构与提纲设计，不是五十章正文。

## 定位与读者

本书讨论软件开发中的隐藏假设、机制与设计取舍。读者可以来自前端、客户端、后端、库与工具开发；基础要求是写过实际功能，能够阅读代码和理解普通程序执行。不预设读者已经熟悉微服务、数据库隔离或分布式协议，这些内容需要在首次展开时建立必要概念。

全书同时解释三种复杂性：问题本身的语义与约束，代码组织和抽象选择引入的责任，以及运行、协作和演进暴露的限制。每章应让读者获得一个可迁移的判断方法，并能说明该方法的适用条件。

先按完整问题设计内容，不以章数和前后端比例裁剪。是否独立成章取决于它是否具有自己的核心问题、概念关系、取舍和验证方式。各章不要求相同篇幅，成稿阶段仍可按论证密度合并或拆分。

## 十个部分的依赖与产出

| 部分 | 章号 | 回答的问题 | 后续所需的基础 |
| --- | --- | --- | --- |
| 值的含义与表示 | 1—7 | 一个值表示什么，怎样转换与检查？ | 身份、单位、缺失含义、转换条件与校验边界 |
| 代码组织与设计取舍 | 8—14 | 怎样划分、共享、隐藏和扩展代码？ | 模块契约、依赖、副作用、资源所有权 |
| 状态、交互与数据变化 | 15—20 | 用户意图如何成为状态，哪些变化可见、可撤销？ | 状态转换、草稿与事实、操作权限、读取承诺 |
| 异步、失败与重复执行 | 21—26 | 工作何时完成，结果交给谁，失败后怎么办？ | 调度、取消、错误传播、预算与重复效果 |
| 并发与共同约束 | 27—30 | 操作交错时如何保护共同条件？ | 不变量、原子提交、隔离、锁与条件写入 |
| 副本、通信与跨边界协作 | 31—36 | 多个参与者如何观察、同步和完成业务？ | 顺序、因果、读取保证、交付、冲突与恢复 |
| 兼容、演进与技术替换 | 37—40 | 已有调用者和存量数据怎样继续工作？ | 兼容方向、迁移阶段、发布条件、替换成本 |
| 性能与有限资源 | 41—44 | 哪种成本值得优化，系统能接受多少工作？ | 工作负载、测量、初始化时机、数据移动与排队 |
| 验证、故障与运行证据 | 45—47 | 怎样证明、反驳和持续检查前面的保证？ | 性质与契约验证、可控故障、关联证据 |
| 把设计组合成完整应用 | 48—50 | 局部选择如何组成用户可以信任的结果？ | 编辑器、文件上传、购买流程三个综合设计 |

本地问题先于跨机问题，定义与承诺先于实现策略。界面状态和编辑先建立要保存什么、怎样反馈，任务调度与取消随后解释实现；并发和同步再处理多方同时修改。性能测量集中整理，但各章在比较方案时仍需说明成本，不把所有代价推迟到性能部分。

## 相邻章节的职责边界

| 相关章节 | 分工 |
| --- | --- |
| 模块、复用、抽象、扩展（8—11） | 模块划分变化范围；复用决定共享规则；抽象定义可见契约；扩展定义外部代码介入的责任。允许同一案例逐步变化，避免四次介绍设计模式。 |
| 依赖、兼容、替换（12、37、40） | 依赖章讨论选择与隔离；兼容章讨论新旧参与者的契约；替换章组织实际迁移和退出路线。 |
| 副作用、资源、初始化（13、14、42） | 分别回答谁修改数据、谁释放资源、何时支付获取和准备成本。 |
| 状态、界面、编辑、修改（15—18） | 分别定义允许的变化、可见反馈、草稿与历史、持久化修改和删除语义。撤销不自动等于远端回滚。 |
| 校验、授权、并发（7、19、27—30） | 输入符合规则、主体有权操作与提交时条件仍成立是不同判断；不能互相替代。 |
| 异步、取消、错误、超时（21—24） | 分别讨论结果交付、停止协议、失败含义和等待预算；取消信号不等于停止已提交的业务。 |
| 重试、幂等、消息（25、26、34） | 再次尝试的策略、重复的业务效果和交付确认是三个层次；消息章直接使用前文模型。 |
| 查询、事务、一致性（20、28、32） | 查询建立分页和快照需求，事务解释数据库内的隔离，一致性规定多副本操作的观察结果。 |
| 顺序、离线、恢复（31、35、36） | 顺序提供判断依据；离线协调同一业务数据的多份修改；恢复组织跨系统的部分成功、补偿和对账。 |
| 缓存、初始化、数据布局、容量（33、42—44） | 分别讨论副本有效性、准备时机、单任务数据成本和多任务接收限制。 |
| 测试、故障验证、观测（45—47） | 分别组织保证与输入、失败与交错实验、运行时证据。测试通过不等同于真实用户验证。 |

已有“版本”入口继续引导到各职责章节，不重建一章混合数据修订号、API 版本与配置版本。幂等与去重、顺序与跨机时钟、日志与指标追踪继续合章；允许增加章节不意味着重新拆回重复概念。

## 案例与端侧覆盖

按问题选择场景，不强制每章同时安排前端和后端例子。每个主要原理应至少有一个改变条件的例子，检验能否迁移；不只更换技术名词或业务名称。

- 代码设计部分以表单规则、文件处理器、插件、库与命令行工具为主要场景。公共组件和服务模块都能出现，但不默认以抽成微服务结束。
- 状态与异步部分以编辑、搜索、导出、页面切换和后台工作为主要场景，再延伸到网络操作。
- 并发与副本部分同时考虑单线程异步交错、线程竞争、双端同步和跨服务协作，分别说明边界，不把机制混用。
- 演进覆盖函数与组件、包依赖、移动客户端、本地文档格式和服务端协议；性能覆盖启动、交互、计算、I/O 与服务负载。
- 三个综合案例分别突出持续交互、资源处理和跨系统业务。购买案例不承担全书唯一的收束职责。

## 每章的成稿验收

每章需要能回答：要作出什么决定；哪些条件会改变选择；候选方案分别保持什么、付出什么；错误如何被观察；如何验证结论。提纲中的任务、开篇场景、验证重点和必要的内容边界用于约束写作，不要求成稿按同一模板罗列。

不得用工具安装步骤填补论证，也不得把某种框架的默认行为写成语言或架构的普遍规则。经典原则、性能直觉和参考书中的结论都要核对成立条件。实现依赖具体产品时注明范围，并使用官方资料或可复现实验作为依据。

验证贯穿每章，第九部分只负责系统整理。综合实践应回引原理，不能到最后才首次解释幂等、同步或权限。读者是否能迁移所学，需要实际试读与练习反馈；编辑自查只能作为一层证据。

## 分册候选与依赖处理

当前仍维护一份连续目录，不立即决定册数或重新编号。待各部分成稿后，按篇幅、知识依赖和读者使用方式评估。

| 候选 | 分配 | 编辑时需要补充的衔接 |
| --- | --- | --- |
| 两册 | 上册第 1—26 章：数据、代码、交互与执行；下册第 27—50 章：并发、协作、演进与验证 | 上册需要一个只使用本册知识的局部编辑器案例；下册开篇回顾契约、任务、错误和幂等。 |
| 三册 | 第一册第 1—20 章：数据与程序设计；第二册第 21—36 章：执行与协作；第三册第 37—50 章：演进、性能与工程验证 | 每册有独立范围说明和前提索引；综合实践可拆成逐册递进版本，最终版再组合完整知识。 |

不能直接把本地编辑器综合章原样放到第一册末尾：其完整版本依赖后面的并发、同步和迁移。第一册局部版本只展示草稿、状态和可撤销历史，明确哪些网络保证尚未建立。已有时间章的综合练习可以作为局部收束的样例。

## 参考书的借鉴方式

《软件开发中的决策：权衡与取舍》的第 2、4、6、9 章启发了复用、扩展、抽象和依赖主题；第 5 章帮助补充以证据选择优化目标的视角，第 12、13 章帮助拓宽兼容与技术替换的范围。其异常处理主题补入错误章。

借鉴的是问题选择和条件变化下的比较方法。新章采用自己的业务场景、论证结构和验证材料，不复制原书案例、代码或表述。参考书仍有大量 Java 与服务端语境，不能用其目录代替本书的端侧覆盖设计；正式引用和具体事实核验在成稿时完成。

## 文件与发布约定

保留已经存在的页面地址与第一章新文件名的 URL 映射；`SUMMARY.md` 驱动章号、分部和连续阅读。新主题使用稳定的语义目录，不通过批量搬迁旧文件制造无关改动。历史路径中的数字不代表当前顺序。旧合并入口继续存在并链接现行章节。
