# 第二章结构设计：ID 不只是数据库主键

本文件保存作者侧的章节结构、依赖关系和编辑依据，不作为书稿正文发布。正文入口为[第二章](manuscript/part-01-values/02-identity.md)。

## 调整结论

原方案的九节覆盖面完整，但“唯一性”“稳定性”以及“综合设计”“练习回顾”存在相邻和重复。正式书稿收束为七节：保留知识范围，减少目录层级，让读者沿着“识别对象 → 建立保证 → 选择实现 → 应对变化 → 综合验证”的路径前进。

七节结构如下：

1. **这个 ID 在识别什么？**
2. **唯一性与稳定性：身份在哪些变化中保持成立？**
3. **生成方式：自增、随机与按时间排序各保护什么？**
4. **表示与暴露：能引用不等于有权限**
5. **迁移、合并与重建：身份如何穿过变化？**
6. **一次请求里不止一个 ID**
7. **综合设计、练习与本章回顾**

第 1 节与第 2 节必须分开：前者确定“识别谁”，后者确定“在哪个边界和生命周期内保持可辨认”。第 7 节是章末收束，不再把综合设计和练习拆成两个独立小节。

## 核心命题

业务身份需要在存储位置、表示形式和系统边界变化后仍可辨认；数据库主键只是实现这个目标的一种局部手段。

本章不从“哪一种 ID 最好”开始，而是先回答：**我们究竟要让谁保持可辨认？** 随后建立唯一性范围、稳定性、可预测性、可读性、生成位置和生命周期等比较维度，再把这些维度用于常见生成方案和真实变化场景。

本章不声称已经实现同步或运行应用测试。生成算法、跨域唯一性、并发合并和权限控制只在本章所需范围内说明，完整的重试、幂等、并发控制和授权机制留给后续章节。

## 开篇场景：手机和电脑上的 `id = 1` 是同一条待办吗？

沿用离线待办案例：手机和电脑各自从本地第一条记录开始编号，网络恢复后出现两个 `id = 1`。代码分别满足本地规则，却无法判断两条记录是同一条待办的副本，还是两个不同对象。

这个场景一次引出本章的主线：编号在哪个范围内唯一，换设备后是否仍代表同一个对象，用户看到的链接是否应直接使用该编号，以及同步请求和待办本身是否需要不同标识。

## 本章阅读路径

### 2.1 这个 ID 在识别什么？

从待办、订单和一次请求开始，区分实体身份、数据库主键、公开标识、请求标识和关联标识。沿用现有 2.1 正文中的“改名、复制、同步、删除后重建、恢复”检查，先建立业务身份连续性的约定，再解释字段职责。

本节需要明确：分类是按识别对象、存储约束和使用者划分的，不能混成互斥枚举；一个值可以承担多个角色，也可以由多个值通过映射共同指向一个实体。

### 2.2 唯一性与稳定性：身份在哪些变化中保持成立？

把原 2.2 与 2.3 合并，连续回答两个问题：唯一性在哪个作用域成立（单表、单库、单租户、全系统、跨区域，或只在一段时间内），以及对象搬家、分片、备份恢复、重建或导入导出后哪些值必须保持不变。

这一节建立“范围”和“生命周期”两个坐标。局部自增值可以在单表内很好用，却不能自动承担跨库身份；稳定身份也不是任何字段都永远不能改变，而是要明确哪些变化仍指向同一个对象。

### 2.3 生成方式：自增、随机与按时间排序各保护什么？

在前一节的坐标系上比较三类方案，并让每种方案都回答同样的四件事：唯一性边界、冲突处理、排序或可预测性、引入的新运维状态。

1. 自增整数：局部顺序清晰、索引紧凑；代价是规模暴露、中心分配依赖和跨库合并规则。
2. UUID 一类的随机标识：无需中心协调，适合跨边界生成；代价是长度、可读性、索引局部性和排查体验。
3. Snowflake 一类的结构化标识：组合时间、节点和序列；代价是时钟回拨、节点配置、位宽和生成时间泄露。

业务编号作为表示需求的一部分简短处理，强调它通常应与内部身份分离，而不是要求一个字段同时满足机器定位和人工沟通。

### 2.4 表示与暴露：能引用不等于有权限

讨论标识出现在 URL、日志、错误信息、导出文件和客户端缓存中的含义。区分可读性、可预测性、稳定公开引用和安全边界：不可枚举标识可以降低遍历风险，却不能替代认证、租户隔离和对象级授权。

本节只处理“标识被谁看到、用来怎样引用”的问题，不展开迁移映射，也不提前讲完整授权模型。结论回到请求路径：先验证调用者，再检查其是否有权访问该实体。

正文已完成：[表示与暴露](manuscript/part-01-values/02-identity/04-representation-exposure.md)。以转发订单链接为主线，依次说明公开表示约定、对象级授权、分享凭证与标识的区别，以及日志、错误信息、导出和缓存中的暴露边界；技术依据链接 OWASP 原始指南。

### 2.5 迁移、合并与重建：身份如何穿过变化？

用订单库拆分、两个租户合并、外部系统导入和灾备重建检验前面的约定。引入旧新 ID 映射、稳定外部 ID、别名表、来源标识、冲突处理和迁移期间的双读/双写边界。

这是本章最重要的综合推导：唯一性解决“会不会撞”，稳定性解决“搬家后是不是同一个”，映射解决“旧证据怎样找到新位置”。本节不把迁移写成数据库操作教程，而是用它检验身份设计是否自洽。

### 2.6 一次请求里不止一个 ID

整理实体 ID、请求 ID、幂等键、链路追踪 ID 和外部交易号的关系。用“支付响应丢失后客户端重试”的小案例说明：请求标识识别调用尝试，幂等键约束重复业务效果，订单 ID 识别业务实体，支付平台交易号支持跨系统对账。

这些值可以出现在同一日志行，却不能互相替代。本节只建立概念接口，为后续超时、重试和幂等章节留出边界，不在此处展开完整机制。

### 2.7 综合设计、练习与本章回顾

以订单系统给出一套可复核的标识设计：内部主键、公开订单号、请求幂等键、支付交易号和迁移映射表分别由谁生成、在哪个边界唯一、保存多久、是否允许更换，以及失败时保留什么证据。

随后用三个练习检查同一组问题：把单库订单拆成多区域、把用户数据导入新系统、把公开 URL 从可枚举编号改为稳定标识。每题要求写出识别对象、唯一性范围、生成方、迁移策略和授权检查，而不是只选择一种 ID 类型。

章末回到下一章：身份稳定之后，金额仍可能因精度、币种和舍入规则不同而悄悄改变含义。

## 为什么这样安排

```text
识别对象
  → 唯一性与稳定性
  → 生成策略
  → 表示与暴露
  → 迁移与合并
  → 请求与跨系统关联
  → 综合设计与验证
```

这样调整有三点目的：

1. 把“身份是什么”和“身份在哪些边界内成立”分开，避免抽象概念互相覆盖。
2. 把实现选择放在语义坐标之后，避免把 UUID、自增 ID 和 Snowflake 写成脱离场景的产品清单。
3. 把迁移、请求案例和练习作为三种验证方式，分别检查存储变化、重复调用和完整方案，而不是再增加平行概念。

## 篇幅与编辑控制

- 2.1 负责建立词汇和业务身份，不在此处比较算法。
- 2.2 只讲唯一性范围、稳定性和生命周期，避免与迁移案例重复展开。
- 2.3 只比较生成方案，不把索引调优写成独立主题。
- 2.4 只讲表示、暴露和授权边界，不把“不可枚举”写成安全保证。
- 2.5 承担全章主要工程场景，必要时可最长，但必须围绕身份映射组织。
- 2.6 只说明各种请求相关标识的职责，重试和幂等细节移交后文。
- 2.7 同时完成综合案例、练习和回顾，避免章末重复总结。

如果正文仍然过长，优先压缩 2.3 的算法比较和 2.7 的练习说明，不要删掉 2.1 的身份定义或 2.5 的迁移检验；这两部分分别承担本章的语义起点和现实验证。

## 验证重点

- 同一实体在迁移、重建和跨服务关联后仍能被确认；数据库位置变化不会悄悄生成第二个业务身份。
- 唯一性声明带有明确范围；不同租户、区域和生成节点不会被无条件当作全局唯一。
- 公开标识、内部主键和请求标识各自承担清楚的职责；知道一个标识不能跳过认证与授权。
- 生成策略的排序、可预测性、时钟依赖、索引代价和运维成本都能被说明，而不是只报出算法名称。
- 章末订单设计能够同时解释实体定位、公开引用、重复请求、跨系统对账和迁移后的历史证据。

本轮已完成正文：第二章第六节《一次请求里不止一个 ID》，正文入口为 [06-request-identifiers.md](manuscript/part-01-values/02-identity/06-request-identifiers.md)。
