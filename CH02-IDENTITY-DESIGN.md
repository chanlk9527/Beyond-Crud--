# 第二章结构设计：ID 不只是数据库主键

本文件保存作者侧的章节结构、依赖关系和编辑依据，不作为书稿正文发布。正文入口为 [第二章](manuscript/part-01-values/02-identity.md)。

## 当前落地与第一节层级

章首页与 [2.1 这个 ID 在识别什么？](manuscript/part-01-values/02-identity/01-identity.md)已有正文，其余节仍为结构方案。

2.1 按“属性变化中的身份 → 数据库记录 → 公开引用 → 操作身份 → 检查约定”展开，分别对应 2.1.1—2.1.5。改名、同名、复制、同步和恢复使用同一个待办案例，先建立身份连续性的业务约定，再解释字段职责，避免开头连续堆叠名词。主键与公开标识是使用角色，实体与请求是被识别对象，两种分类不能混成互斥枚举；一个值可以承担多个角色。

本节通过操作预期表检查身份语义，不声称已实现同步或运行了应用测试。生成算法、跨域唯一性、并发合并和权限控制留给后续相应内容。初稿中的生成策略比较仍需逐项查证：UUID 要区分版本，编号分配顺序不等于业务提交顺序，不能将全部 UUID 归为随机标识。

> 核心命题：业务身份需要在存储位置、表示形式和系统边界变化后仍可辨认；数据库主键只是实现这个目标的一种局部手段。

## 本章任务

这一章不从“哪一种 ID 最好”开始，而是先回答一个更早的问题：**我们究竟要让谁保持可辨认？**

读者将依次区分实体身份、数据库主键、公开标识、请求标识和关联标识，建立判断标识方案所需的几个维度：唯一性范围、稳定性、可预测性、可读性、生成位置与生命周期。随后再把这些维度用于比较自增 ID、UUID、Snowflake 和业务编号，并检验它们在拆库、迁移、合并、重建和对外暴露时的边界。

本章最后用一个跨存储迁移的订单案例，把“身份仍是同一个”与“记录被搬到了哪里”分开；再把标识和认证、授权、幂等请求联系起来，避免读者把“知道一个 ID”误认为“有权操作它”。

## 开篇场景：手机和电脑上的 `id = 1` 是同一条待办吗？

打开一个普通的待办应用：手机还没有联网，电脑也刚刚装好应用。你在手机上写下“买牛奶”，在电脑上写下“给房东发消息”。两个设备都从本地的第一条记录开始编号，于是它们都生成了 `id = 1`。网络恢复后，应用把两边的数据同步到一起；列表里出现了两条 `id = 1`，编辑、删除和分享链接开始互相指错。

代码并没有违反各自设备上的规则。问题在于，`1` 只在本地存储里唯一，却被同步逻辑当成了所有设备都认可的身份。这个场景一次引出四个问题：一个编号在哪个范围内唯一，换设备后是否仍代表同一个待办，用户看到的链接是否应该直接使用这个编号，以及同步请求和待办本身是否需要不同的标识。先拆开这些问题，再讨论生成算法，能避免把“数据库里能定位一行”误当成“系统里能辨认一个对象”。

## 本章阅读路径

### 2.1 先问：这个 ID 在识别什么？

从订单、用户和一次请求三个例子开始，区分以下关系：

```text
实体身份       → 现实中要持续辨认的对象
数据库主键     → 某个存储系统内部定位一条记录的键
公开标识       → 交给客户端、URL 或人工沟通的表示
请求标识       → 识别一次调用或一次尝试
关联标识       → 把多个系统中的记录串成一条证据链
```

本节建立全章的分类坐标。它放在最前面，是因为后面的生成策略只有在“标识对象”明确后才有比较意义；例如，订单主键和支付请求的 `requestId` 都可能是字符串，却承担不同的生命周期与重复语义。

### 2.2 唯一，不是一个没有范围的形容词

讨论唯一性的作用域：单表、单库、单租户、全系统、跨区域，以及“在某段时间内唯一”。说明局部自增值为什么可以在单表内很好用，却不能自动承担跨库身份；也说明租户内编号和全局编号并不互相替代。

本节的层级关系是：先确定对象，再确定要在哪个边界内不冲突。只有边界明确，生成算法的成本和保证才可计算。

### 2.3 稳定性：记录搬家以后，什么必须不变？

区分“数据库行的位置”“业务对象的身份”和“用户看到的链接”。沿着迁移、分片、备份恢复、数据重建和导入导出等变化，说明哪些值可以重新生成，哪些值一旦变化就会断开外部引用、历史审计或跨系统关联。

本节把核心命题具体化：**稳定身份不是永远不能变，而是要明确哪些变化仍指向同一个对象。** 这为后面的 ID 映射表、别名和合并策略提供前提。

### 2.4 生成方式：自增、随机与按时间排序各保护什么？

在前面坐标系上比较三类常见方案：

1. 自增整数：生成简单、索引紧凑、天然有局部顺序；代价是暴露规模、依赖单点分配，跨库合并需要额外规则。
2. UUID 一类的随机标识：无需中心协调，适合跨边界生成；代价是可读性、索引局部性、长度和排查体验。
3. Snowflake 一类的结构化标识：把时间、节点和序列组合起来，兼顾分布式生成与排序；代价是时钟回拨、节点配置、位宽和泄露生成时间等新的约束。

这里不把方案写成产品清单，而是每种方案都回答三件事：它在哪个唯一性边界内成立，如何处理生成冲突，带来了什么新状态。业务编号另设一小节，说明“可读的订单号”通常应与内部身份分离，而不是要求一个字段同时满足机器定位和人工沟通。

### 2.5 表示与暴露：能猜到 ID，和有权访问是两回事

讨论 URL、日志、错误信息、导出文件和客户端缓存中的标识暴露。说明不可枚举标识可以降低遍历风险，却不能替代认证、授权、租户边界和对象级权限检查；可读编号也可能是客服、财务或用户流程所需的产品能力。

本节放在生成策略之后，是因为“是否暴露”取决于标识的预测性、语义和使用者，而不是只取决于它是不是 UUID。结论会回到一条请求路径：先验证调用者，再检查其是否有权访问该实体。

### 2.6 迁移、合并与重建：身份如何穿过变化？

用订单库拆分、两个租户合并、外部系统导入和灾备重建四个变化检验前面的约定。引入必要的工程工具：旧新 ID 映射、稳定外部 ID、别名表、来源标识、冲突处理和迁移期间的双读/双写边界。

本节是全章的综合推导：唯一性解决“会不会撞”，稳定性解决“搬家后是不是同一个”，映射解决“旧证据怎样找到新位置”。把三者放在同一条迁移链上，读者才能看出它们不是三个互不相干的技巧。

### 2.7 一次请求里不止一个 ID

整理实体 ID、请求 ID、幂等键、链路追踪 ID 和外部交易号的关系。用“支付响应丢失后客户端重试”的小案例说明：请求标识用于识别调用尝试，幂等键用于约束重复业务效果，订单 ID 用于识别业务实体，支付平台交易号用于跨系统对账。它们可能被记录在同一日志行，却不能互相替代。

本节为后续“超时、重试、幂等”章节留下明确接口：本章只说明不同标识各自识别什么，不提前展开重复执行的完整机制。

### 2.8 综合设计：一套同时服务数据库、用户和外部系统的标识

以订单系统为例，给出一份可复核的设计：内部主键、公开订单号、请求幂等键、支付交易号、迁移映射表分别由谁生成、在哪个边界唯一、保存多久、是否允许更换，以及失败时保留什么证据。

这一节不是把前文再列一遍，而是让读者用同一组问题检查一套设计是否自洽：识别对象是什么，唯一性范围多大，生命周期多长，能否迁移，暴露后是否仍需授权，事后如何关联记录。

### 2.9 设计练习与本章回顾

练习围绕三个变化展开：把单库订单拆成多区域、把用户数据导入新系统、把公开 URL 从可枚举编号改为稳定标识。每题要求写出对象、唯一性范围、生成方、迁移策略和授权检查，而不是只选择一种 ID 类型。

章末留下下一章的问题：即使身份已经稳定，金额这个值在不同精度、币种和舍入规则下仍可能代表不同结果。**识别同一个对象之后，我们还要保证保存和计算的值没有悄悄改变含义。**

## 为什么这样铺开

本章的顺序遵循一个由语义到实现、再回到系统变化的依赖链：

```text
识别对象
  → 唯一性范围
  → 稳定性与生命周期
  → 生成策略
  → 表示与暴露
  → 迁移与合并
  → 请求与跨系统关联
  → 综合设计与验证
```

这样安排有三个理由。第一，读者先获得比较方案的共同坐标，避免一上来把 UUID 与自增 ID 当成脱离场景的“选型题”。第二，迁移和请求案例分别检验“身份穿过存储变化”和“多个动作围绕同一实体协作”这两种容易混淆的关系，知识结构因此闭合。第三，授权只在标识暴露之后出现，既能解释不可枚举 ID 的实际边界，也不会把身份、认证和权限混成一个概念。

## 验证重点

- 同一实体在迁移、重建和跨服务关联后仍能被确认；数据库位置变化不会悄悄生成第二个业务身份。
- 唯一性声明带有明确范围；不同租户、区域和生成节点不会被无条件当作全局唯一。
- 公开标识、内部主键和请求标识各自承担单一职责；知道一个标识不能跳过认证与授权。
- 生成策略的排序、可预测性、时钟依赖、索引代价和运维成本都能被说明，而不是只报出算法名称。
