
> **从“单机、静态、确定”的理想世界，逐步走向“并发、分布式、变化、不确定”的真实世界。**

这样它就不是“时间一篇、ID一篇、缓存一篇”的技术博客合集，而是一套递进的工程认知体系。

# 《软件系统的隐藏复杂性》

### 从时间、状态、重试到并发与分布式系统

## 第一部分：那些“看起来只是一个字段”的问题

这一部分先建立全书的基本认知：

> 很多软件问题，真正复杂的不是代码，而是**语义**。

### 第 1 章：时间不是一个 `datetime`

讲：

* 时间点与持续时间
* UTC 与本地时间
* 时区
* 夏令时
* Unix Timestamp
* 数据库时间类型
* 用户自然日
* 定时任务
* 过期时间
* 客户端时间是否可信
* 多机器时钟偏差

核心思想：

> **先问“这个时间代表什么”，再问“怎么存”。**

---

### 第 2 章：ID 不只是数据库主键

讲：

* 自增 ID
* UUID
* Snowflake
* 业务 ID
* 数据库主键与业务标识
* 跨库、跨区域唯一性
* ID 是否应该暴露给用户
* ID 是否需要稳定
* 数据迁移时 ID 怎么办

核心思想：

> **身份和存储位置是两回事。**

---

### 第 3 章：金额、精度与舍入

讲：

* 为什么不能随便用 float
* Decimal
* 最小货币单位
* 汇率
* 四舍五入
* 税费
* 百分比
* 金额计算顺序

核心思想：

> **数学上差不多，在业务上可能完全不一样。**

---

### 第 4 章：字符串、编码与“长度”

讲：

* Unicode
* UTF-8
* 字节与字符
* emoji
* 大小写
* Unicode normalization
* 字符串比较
* 排序
* 文件名
* 用户名

核心思想：

> **字符串不是简单的字符数组。**

---

### 第 5 章：Null、空值与默认值

讲：

```text
null
""
0
false
[]
字段不存在
默认值
```

它们到底是否等价。

延伸到：

* PATCH
* 数据库 nullable
* protobuf optional
* JSON
* 配置系统

核心思想：

> **“没有值”和“值为空”是不同的状态。**

---

# 第二部分：数据不是静态的

第一部分讨论“一个值是什么”。

这一部分开始讨论：

> **值会变化。**

---

### 第 6 章：状态与状态机

讲：

* 状态字段
* 状态转换
* 非法状态跳转
* 中间状态
* 终态
* 状态机
* 状态与事件
* 状态爆炸

例如：

```text
待支付
→ 已支付
→ 发货中
→ 已完成
```

为什么不能简单写成几个 `if`。

核心思想：

> **真正复杂的业务，复杂的通常不是数据，而是状态变化。**

---

### 第 7 章：创建、更新与删除

讲：

* CRUD 并没有想象中简单
* 覆盖更新 vs 部分更新
* PUT vs PATCH
* 逻辑删除
* 物理删除
* 级联删除
* 恢复
* 数据保留期
* TTL

核心思想：

> **删除也是一种状态变化。**

---

### 第 8 章：版本

讲：

* 数据版本
* API 版本
* 配置版本
* 文件版本
* 乐观锁 version
* Schema version

第一次引入非常重要的思想：

> **系统中的“同一个东西”可能同时存在多个版本。**

这会为后面的兼容性铺路。

---

# 第三部分：事情可能不只发生一次

这是整本书非常关键的一次跃迁。

前面默认：

```text
请求一次
→ 执行一次
→ 得到结果
```

现实世界开始告诉你：

> 并不是。

---

### 第 9 章：超时

讲：

```text
Client
  │
  │ Request
  ↓
Server
  │
  │ 已执行成功
  ↓

但 Response 丢了
```

客户端看到：

```text
timeout
```

那么：

> **到底成功了没有？**

这一章是后面很多问题的起点。

---

### 第 10 章：重试

讲：

* 什么错误应该重试
* 什么错误不能重试
* 固定间隔
* 指数退避
* jitter
* retry storm
* retry budget

核心思想：

> **重试可以提高可靠性，也可以把一个小故障变成雪崩。**

---

### 第 11 章：幂等

紧接着重试。

因为：

> 一旦允许重试，就必须考虑重复执行。

讲：

* Idempotency Key
* requestId
* 唯一约束
* 状态检查
* 幂等接口
* 支付
* 创建订单
* 文件上传
* MQTT 消息

核心思想：

> **调用次数和业务执行次数必须解耦。**

---

### 第 12 章：去重

进一步区分：

```text
幂等
≠
去重
```

讲：

* 消息去重
* eventId
* duplicate window
* 去重表
* TTL
* Exactly Once 的幻觉

---

# 第四部分：世界不只有一个线程

前面的问题还是串行的。

现在开始：

> **两个操作可以同时发生。**

---

### 第 13 章：竞态条件

从最简单例子开始：

```text
库存 = 1

线程 A 读取：1
线程 B 读取：1

A 扣减
B 扣减
```

讲：

* race condition
* lost update
* check-then-act
* TOCTOU

核心思想：

> **单独看每段代码都正确，并不代表并发执行也是正确的。**

---

### 第 14 章：锁

讲：

* mutex
* synchronized
* 数据库锁
* 悲观锁
* 分布式锁
* 锁粒度
* 死锁
* 锁超时

重点不是教 Redis RedLock API，而是：

> **为什么要锁，以及锁保护的究竟是什么。**

---

### 第 15 章：乐观并发控制

讲：

* CAS
* version
* compare-and-swap
* ETag
* If-Match
* 数据库乐观锁

让读者理解：

> **并发控制不一定意味着“把别人挡住”。**

---

### 第 16 章：事务与原子性

讲：

* ACID
* transaction boundary
* rollback
* 隔离级别
* dirty read
* non-repeatable read
* phantom read

重点还是工程含义，而不是数据库教材式背定义。

---

# 第五部分：世界不只有一台机器

这是从普通后端开发进入分布式系统的关键部分。

---

### 第 17 章：缓存——为什么两份数据一定会不一致

讲：

```text
DB
+
Redis
```

然后出现：

* 先删缓存还是先更新 DB
* cache aside
* TTL
* 缓存击穿
* 缓存雪崩
* 缓存穿透
* stale data

但真正核心的是：

> **只要存在两份状态，一致性问题就出现了。**

---

### 第 18 章：消息——延迟、重复、乱序与丢失

讲：

* MQ
* producer
* consumer
* ack
* 重复消费
* 消息丢失
* consumer retry
* dead letter
* 顺序消息
* consumer lag

把之前的：

```text
超时
重试
幂等
去重
顺序
```

全部重新串起来。

读者会发现：

> 前面的章节其实一直在给 MQ 做准备。

这就是“书”而不是博客合集。

---

### 第 19 章：顺序

单独拿出来讲。

因为很多程序员会默认：

```text
A 先发生
B 后发生
```

那么所有机器都一定看到：

```text
A → B
```

实际上未必。

讲：

* 时间顺序
* 请求顺序
* 消息顺序
* 数据库提交顺序
* sequence number
* version
* logical clock

核心思想：

> **现实发生顺序、发送顺序、到达顺序、处理顺序不是一回事。**

---

### 第 20 章：一致性

终于进入：

* 强一致性
* 最终一致性
* read-your-writes
* consistency window
* 双写
* 补偿
* Saga
* outbox

但不必写成纯分布式理论书。

重点回答：

> **当两个系统必须共同完成一件事，但你又不能真正把它们放进一个事务里，该怎么办？**

---

### 第 21 章：分布式系统中的时间

这里再次回到第 1 章。

但此时已经完全不同。

第一章讨论：

> 业务时间。

这里讨论：

* clock skew
* NTP
* server timestamp
* logical clock
* Lamport Clock
* ordering
* timestamp 是否能判断事件先后

形成第一次非常明显的**首尾呼应**。

让读者意识到：

> 原来第一章的时间问题，一进入分布式系统，又变了一个层次。

---

# 第六部分：系统永远处于变化中

前五部分解决的是：

> 系统运行时会发生什么。

最后一部分解决：

> **系统本身也会变化。**

---

### 第 22 章：API 兼容性

讲：

* 新增字段
* 删除字段
* 字段语义变化
* enum 新值
* required / optional
* 老客户端
* API versioning

核心思想：

> **接口最大的敌人不是错误，而是未来。**

---

### 第 23 章：数据结构演进

讲：

* database migration
* schema evolution
* nullable transition
* backfill
* 双写
* expand / migrate / contract

例如：

```text
旧字段
    ↓
新旧并存
    ↓
迁移数据
    ↓
切换读路径
    ↓
停止旧写入
    ↓
删除旧字段
```

---

### 第 24 章：配置与灰度

讲：

* 配置
* 默认值
* 环境差异
* feature flag
* 灰度
* rollout
* rollback
* 配置版本
* 配置传播延迟

这里再次出现：

```text
版本
一致性
缓存
延迟
状态
```

前面的知识又重新汇合。

---

### 第 25 章：文件与大对象

这是一个非常实用的综合章节：

* 文件上传
* Content-Type
* 文件大小
* Hash
* checksum
* 分片
* multipart upload
* 断点续传
* 重试
* 幂等
* Range
* 对象存储
* 生命周期
* CDN

它实际上是前面很多知识的综合应用。

---

# 第七部分：系统出问题以后，你怎么知道

最后进入可观测性。

---

### 第 26 章：错误到底是什么

讲：

* 用户错误
* 业务错误
* 系统错误
* transient error
* permanent error
* error code
* exception
* HTTP status

以及：

> 什么错误应该重试？

自然与前面的重试章节连接。

---

### 第 27 章：日志

讲：

* 日志级别
* 结构化日志
* traceId
* requestId
* deviceId / userId
* 敏感信息
* 日志采样

重点：

> **日志不是写给机器看的流水账，而是事故发生之后留下的证据。**

---

### 第 28 章：Metrics、Trace 与可观测性

讲：

```text
Logs     → 发生了什么
Metrics  → 整体是否异常
Trace    → 一次请求经过了哪里
```

以及：

* latency
* percentile
* error rate
* saturation
* tracing
* correlation ID

---

# 最后一部分：把所有问题重新放到一起

我会专门留两章。

## 第 29 章：一个请求的一生

用一个完整业务串起全书。

例如：

> 用户点击“购买云存储套餐”。

然后依次经历：

```text
用户点击
↓
API
↓
认证
↓
幂等
↓
数据库事务
↓
调用支付平台
↓
timeout
↓
retry
↓
MQ
↓
重复消息
↓
更新权益
↓
Redis
↓
App 查询
↓
日志 / Metrics / Trace
```

然后问：

* 时间怎么算？
* orderId 怎么生成？
* 金额怎么表示？
* 请求重复怎么办？
* 支付超时怎么办？
* MQ 重复怎么办？
* 权益状态是什么？
* DB 和 Redis 不一致怎么办？
* 老版本 App 怎么办？

**一章把整本书重新串起来。**

---

# 第 30 章：软件工程的几个根本问题

最后不再讲技术。

而是把 30 章压缩成几个原则。

比如：

### 1. 语义比类型重要

```text
Long timestamp
```

没有告诉你它真正是什么。

---

### 2. 不要默认事情只发生一次

所以才有：

```text
幂等
去重
重试
```

---

### 3. 不要默认事情按顺序发生

所以才有：

```text
version
sequence
ordering
```

---

### 4. 不要默认所有人看到的是同一个世界

所以才有：

```text
缓存一致性
最终一致性
分布式系统
```

---

### 5. 不要默认成功和失败是二元状态

还有一种非常重要的情况：

```text
UNKNOWN
```

这就是 timeout 最麻烦的地方。

---

### 6. 不要默认软件不会变化

所以才有：

```text
版本
兼容
迁移
灰度
回滚
```

---

最后整本书其实就在回答一个问题：

> **当我们在真实世界里构建软件时，应该警惕哪些“理所当然”的假设？**

这比“100 个开发知识点”高一个层次。

所以如果让我现在定稿，我会把整本书概括成：

```text
第一部分  值没有那么简单
第二部分  状态会变化
第三部分  事情可能重复发生
第四部分  事情可能同时发生
第五部分  事情可能发生在不同机器
第六部分  系统本身也会变化
第七部分  出问题以后如何理解系统
第八部分  把所有问题重新连接起来
```


