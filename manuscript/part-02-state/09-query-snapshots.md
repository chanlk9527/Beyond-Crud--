# 第 20 章：查询、分页与快照

> 核心命题：多次查询是否属于同一批数据，需要额外的读取约定。

## 本章任务

从列表和文件导出推导稳定排序、唯一补充键、偏移与游标分页；区分遍历变化的数据与读取固定快照，处理筛选变化、游标上下文和统计口径。事务章解释快照实现，一致性章解释跨副本读取。

## 开篇场景

待写：不断插入新内容的列表出现重复，导出结束后才发现缺少部分记录。

## 验证重点

插入、删除和排序字段变化期间检查重复及遗漏；换筛选条件或权限范围后不能沿用旧游标。
