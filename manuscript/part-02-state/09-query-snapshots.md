# 第 9 章：查询、分页与快照

> 核心命题：多次查询是否属于同一批数据，需要额外的读取约定。

## 本章任务

从稳定排序和唯一的排序补充键推导偏移分页与游标分页；区分遍历变化中的列表和读取固定快照，解释筛选变化、游标上下文、导出和统计口径。事务快照的实现留到事务章，跨副本读取保证留到一致性章。

## 开篇场景

待写：翻到第二页发现重复记录，导出完成后又发现漏掉了一部分订单。

## 验证重点

插入、删除与排序字段更新期间检查重复和遗漏；游标不能跨筛选条件或权限范围复用。
