# 第 8 章：权限与归属：谁能操作这份数据

> 核心命题：授权需要共同判断主体、资源、动作与当时的业务条件。

## 本章任务

区分认证与授权，建立资源归属、租户隔离、动作权限和字段权限；讨论撤销后的缓存、批量操作、后台任务代用户执行与检查时点。标识难猜不能替代权限判断；不展开认证协议和密码学实现。

## 开篇场景

待写：用户把请求中的订单 ID 换成别人的，服务端是否仍会返回订单？

## 验证重点

跨用户、跨租户、撤权后、批处理与后台路径都验证相同边界；拒绝时不得泄露受限数据。
