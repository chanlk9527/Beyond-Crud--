# 第 42 章：初始化、加载与预计算

> 核心命题：初始化和预计算改变成本与失败暴露的时机，并产生新的有效性条件。

## 本章任务

比较急切与延迟初始化、按需加载、预取、预热和预计算；定义就绪、单次初始化、失败重试、失效重算及并发触发。覆盖应用启动、插件加载、索引与昂贵对象构造。

## 开篇场景

待写：为了缩短启动时间延迟加载组件，第一次点击却明显卡顿，初始化失败还无法恢复。

## 验证重点

冷启动、首次使用、重复触发、初始化中失败及输入变化后的重算；启动快不能掩盖首次响应慢。

## 内容边界

生命周期章解释拥有与释放，本章决定何时支付成本；缓存章解释通用副本一致性。
