# 第 27 章：竞态条件与业务不变量

> 核心命题：单独正确的操作交错执行时，仍可能共同破坏业务约束。

## 本章任务

先用旧搜索结果覆盖新结果说明单线程也可能出现逻辑交错，再用共享内存和库存写入解释 lost update、check-then-act 与 TOCTOU。区分数据竞争与更广的竞态，写出不变量后比较代次判断、原子操作与数据库约束。

## 开篇场景

待写：一次检查通过后条件已经变化，搜索结果更新错对象，库存也可能被超卖。

## 验证重点

提供可复现的交错；跨事件循环、线程与数据库时分别说明保证边界。
