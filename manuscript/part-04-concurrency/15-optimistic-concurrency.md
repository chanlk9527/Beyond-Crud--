# 第 30 章：乐观并发控制：拒绝过时的写入

> 核心命题：基于旧状态作出的修改，应在提交时检查它所依赖的前提是否仍成立。

## 本章任务

讨论数据修订号、CAS、ETag、If-Match 和数据库条件更新；区别版本相等与内容相等，分析版本作用域、ABA 风险以及冲突后的拒绝、重试和合并。API 版本与数据修订号不承担相同职责。

## 开篇场景

待写：两位客服同时修改订单，后保存的人怎样避免无声覆盖先前修改？

## 验证重点

并发编辑、陈旧客户端、删除重建及冲突重试都不能绕过条件；多记录约束不能只靠单行版本保证。
