# 第 29 章：锁：保护什么，保护到哪里

> 核心命题：锁只有在所有参与者遵循同一协调规则时才能保护目标资源。

## 本章任务

围绕资源、不变量、粒度和持有区间比较 mutex、synchronized、数据库锁与分布式租约；讨论死锁、超时、续租、持有者暂停和过期后的旧持有者写入。解释资源端校验令牌的条件与边界。

## 开篇场景

待写：服务部署第二个实例后，原来有效的进程内锁为什么挡不住重复扣减？

## 验证重点

多实例竞争、锁超时、进程暂停、旧持有者恢复与异常退出不能绕过目标约束。
