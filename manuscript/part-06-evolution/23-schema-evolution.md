# 第 24 章：数据结构演进

> 核心命题：迁移是新旧结构和存量数据共同运行的一段过程。

## 本章任务

讨论 schema version、可空过渡、回填、双写与 expand/migrate/contract；确定新旧表示的权威来源、切换条件和回退边界，关注在线写入与回填竞争。不能假定所有姓名都能按同一规则拆分。

## 开篇场景

待写：把一个姓名字段拆成多个字段，为什么不能执行 ALTER TABLE 后立即切换所有代码？

## 验证重点

回填可恢复且不覆盖新写入；新旧程序并存、校验失败及回退时保持数据含义。
