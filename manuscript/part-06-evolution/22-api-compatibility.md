# 第 37 章：兼容性：函数、组件与协议的承诺

> 核心命题：兼容性要求新旧参与者共存时，仍能正确理解彼此的契约。

## 本章任务

从函数签名、组件属性和库开始，区分源码、二进制、行为与数据协议兼容；讨论语义版本、菱形依赖、默认值、枚举、字段演进和废弃窗口。覆盖移动端长期旧版本和网络 API，新版本号不能自动提供兼容性。

## 开篇场景

待写：组件升级后仍能编译，却改变了默认排序；旧客户端遇到新枚举时又崩溃。

## 验证重点

旧调用者与新实现、新调用者与旧数据分别验证；编译成功之外还需检查行为和升级路径。
