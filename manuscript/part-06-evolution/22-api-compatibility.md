# 第 23 章：API 兼容性

> 核心命题：兼容性要求新旧参与者共存时，仍能正确理解彼此的契约。

## 本章任务

讨论新增与删除字段、语义变化、枚举新值、required/optional、默认行为和 API versioning；分别检查请求与响应的兼容方向。版本号用于标明契约，不能自动解决兼容问题。

## 开篇场景

待写：服务端新增订单状态后，未升级的客户端为什么崩溃或执行错误动作？

## 验证重点

新旧客户端与新旧服务端组合验证，未知枚举与缺失字段有约定行为。
