# 第 13 章：幂等与去重：重复请求的业务效果

> 核心命题：去重识别重复输入，幂等约束重复执行后的业务效果，两者不能互相替代。

## 本章任务

建立业务意图、请求身份和事件身份；讨论幂等键、参数一致性、唯一约束、处理中状态、结果复用、去重表和过期窗口。解释去重记录与业务写入的原子边界，以及 Exactly Once 声明的保证范围。

## 开篇场景

待写：用户双击购买，又在超时后重新提交，这几次请求表达的是一次购买还是多次购买？

## 验证重点

并发重复、键相同参数不同、写入后崩溃、响应丢失和窗口过期后的重放均有明确结果。
