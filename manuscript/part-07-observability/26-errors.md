# 第 10 章：错误、拒绝与未知结果

> 核心命题：错误模型应帮助调用方判断结果、责任和下一步动作。

## 本章任务

区分业务拒绝、系统失败、暂时性与永久性故障以及结果未知；明确错误码、异常与 HTTP 状态的职责，说明可重试性还依赖操作语义和副作用。为超时与重试建立共同语言。

## 开篇场景

待写：同样返回 HTTP 500，一次操作没有执行，另一次却已经扣款，调用方该怎么办？

## 验证重点

相同错误表示不能掩盖不同业务结果；调用方能区分修改输入、查询结果、等待和重试。
