# 第 6 章：单位、坐标与本地化表示

> 核心命题：数值的含义依赖单位、参照系和表示环境，格式变化不应暗中改变原始量。

## 本章任务

从长度、温度、比例与二维坐标建立单位和参照系；区分换算与显示，讨论精度、缩放、原点、区域格式、语言和排序环境。连接金额、时间与字符串，但不重复其内部算法。

## 开篇场景

待写：地图上的位置偏移了，表单中的 1,234 在另一个地区又被解析成不同数值。

## 验证重点

单位混用、坐标转换、区域切换及格式化往返；显示截断不能覆盖原始精度。

## 内容边界

本章定义值的解释条件；输入是否符合契约交给校验章，不扩展为地图或国际化框架教程。
