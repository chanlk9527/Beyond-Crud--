# 第 5 章：Null、空值与默认值

> 核心命题：未提供、未知、不适用与明确清空表达不同的业务意图。

## 本章任务

辨析 null、空字符串、零、false、空集合、字段不存在和默认值；连接 PATCH、数据库可空列、JSON、Protobuf optional 与配置。协议的缺省规则不能覆盖用户的明确选择。

## 开篇场景

待写：更新个人资料时，不传昵称与传空昵称应该产生相同结果吗？

## 验证重点

省略与清空分别验证；旧数据和旧客户端经过默认值处理后仍保持含义。
