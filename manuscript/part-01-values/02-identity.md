# 第 2 章：ID 不只是数据库主键

> 核心命题：业务身份需要在存储位置和表示变化后仍可辨认。

## 本章任务

区分实体身份、数据库主键、公开标识和一次请求的身份；比较唯一性的范围、稳定性、复用与映射，再以自增 ID、UUID 和 Snowflake 说明实现取舍。标识暴露不等于授权。

## 开篇场景

待写：拆库后订单编号发生冲突，外部链接和历史流水应该指向谁？

## 验证重点

迁移、合并和重建记录后身份保持；不同租户和不同请求的标识不能混用。
