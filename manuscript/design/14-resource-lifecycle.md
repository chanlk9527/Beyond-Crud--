# 第 14 章：资源的所有权与生命周期

> 核心命题：创建资源时也要定义所有者、有效范围与释放责任。

## 本章任务

统一讨论文件句柄、连接、定时器、订阅与工作线程的获取、借用、转移和释放；区分垃圾回收与外部资源清理，处理异常路径、重复关闭和部分初始化。

## 开篇场景

待写：关闭页面后监听器仍运行，失败的导出任务又留下临时文件和占用的句柄。

## 验证重点

成功、失败、提前返回、重复释放及所有者销毁后都没有悬挂使用；可观察地检查泄漏。

## 内容边界

资源章定义拥有关系；取消章处理进行中的工作；初始化章比较获取时机及其成本。
