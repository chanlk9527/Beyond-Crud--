# 第 8 章：模块边界与依赖方向

> 核心命题：模块边界应让内部变化可被局部处理，并让依赖关系能够解释。

## 本章任务

从职责与共同变化的原因划分模块，区分内聚、耦合、依赖方向和运行时调用方向；用界面组件、命令行工具及业务模块说明显式契约、循环依赖与组合入口。

## 开篇场景

待写：修改导出格式，却必须同时修改界面、业务计算和存储实现。

## 验证重点

替换展示或存储后哪些模块需要修改；测试是否必须启动无关组件。

## 内容边界

先建立边界语言；相似代码是否共享归复用章，如何选择公开能力归抽象章。
