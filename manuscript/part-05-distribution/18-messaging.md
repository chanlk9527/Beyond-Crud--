# 第 34 章：消息：交付、处理与确认

> 核心命题：消息被接收、业务处理成功和确认完成是不同阶段。

## 本章任务

对比进程内事件通知与持久化队列的不同交付保证；展开发布、接收、处理、确认与重投，讨论积压、死信和顺序范围。用 Outbox 说明业务写入与发送意图的原子记录，用消费幂等处理确认前崩溃。

## 开篇场景

待写：应用退出前发出的保存通知未被处理，服务端写库成功后也可能没发出消息。

## 验证重点

发布与退出、提交与发送、处理与确认之间分别中断，检查丢失、重复和未处理消息的恢复入口。
