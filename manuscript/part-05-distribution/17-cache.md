# 第 33 章：缓存：副本的新鲜度与失效

> 核心命题：缓存是一份需要明确更新、失效与读取规则的数据副本。

## 本章任务

先从组件派生数据、浏览器资源与本地文件缓存定义缓存键、所有者、有效期和失效，再扩展到服务端 Cache Aside、更新顺序和并发回填。讨论权限、语言和版本是否进入键，TTL、陈旧读、击穿与容量影响。

## 开篇场景

待写：切换账号后仍看到上一位用户的数据；更新源数据后，一次迟到读取又填回旧缓存。

## 验证重点

缓存隔离、依赖变化、读写交错与失效失败；说明陈旧范围，不能用命中率替代正确性。
