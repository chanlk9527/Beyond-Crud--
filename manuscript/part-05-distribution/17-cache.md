# 第 20 章：缓存：副本的新鲜度与失效

> 核心命题：缓存是一份需要明确更新、失效与读取规则的数据副本。

## 本章任务

在一致性模型上讨论 Cache Aside、更新与失效顺序、并发回填、TTL、陈旧读、击穿、雪崩与穿透；比较缓存值和缓存判断结果的期限。容量影响回引第七部分，不能只靠调整 TTL 宣称一致。

## 开篇场景

待写：数据库昵称已经修改，页面仍读到旧值；删除缓存之后，旧值为何还可能被写回来？

## 验证重点

读写交错、失效失败、热点过期与源数据撤销后，检查陈旧范围和下游压力。
