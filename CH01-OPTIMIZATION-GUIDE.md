# 第一章优化指引

本文是第一章改写的唯一现行指引，统一记录目标、实施顺序和验收结果。它取代此前的结构设计、改造清单、验收记录与系统性改写记录；不沿用旧文档的“已完成”判断。全书通用规范继续由 [WRITING.md](WRITING.md)、[VOICE.md](VOICE.md) 和 [ILLUSTRATIONS.md](ILLUSTRATIONS.md) 维护，第一章的具体取舍以本文为准。

## 1. 成书目标与问题诊断

目标是一本系统、严谨、在所声明范围内全面的工程书。叙述可以轻松、活泼、有幽默感，但概念定义、技术断言与推导不能依靠读者猜测。权威性来自可核查的依据、清楚的条件和能够复现的结果，不来自语气或名词密度。

当前第一章覆盖了大量时间问题，却仍有三种结构性缺陷。案例承担过多的章节组织职责，读者主要记住“某种问题怎样修”；基础概念虽然已有汇总，但关系和运算机制仍讲得过短；许多重要条件以段尾提醒出现，与主论证处在同一层，读者难以判断哪些是基础、哪些是应用、哪些是扩展。

上一轮增加概念页、调整目录和重写过渡，只是建立了新的外部结构。真正的改写必须进入每节内部，重新组织定义、机制、推导和例证，不能通过改标题、加开头或补总结宣称完成。

## 2. 全章的核心命题与知识范围

核心命题：软件中的时间由不同语义对象组成；正确设计要选择适当的对象，明确它们的运算与转换条件，并在存储和执行中保持业务含义。

五个知识层次构成全章骨架：

| 层次 | 必须建立的关系 | 读者应获得的能力 |
| --- | --- | --- |
| 语义模型 | 瞬间、日期、当地时刻、当地日期时间、持续时间、日历量、区间和生成规则 | 根据问题选择对象，识别值中没有的信息 |
| 转换与计算 | 当地表示与瞬间的映射；持续时间运算与日历运算；端点约定与集合关系 | 从条件推导算法，解释夏令时和月末的结果 |
| 表示与保存 | 语义对象与字符串、数字、数据库类型之间的映射；原始依据与派生值 | 选择存储策略并定义往返不变量 |
| 业务判断 | 判定时刻、受理事实、持续操作、计划执行和资源保留的关系 | 说明每个期限约束哪个动作，以及保证到哪里为止 |
| 来源与验证 | 事件含义、记录来源、时钟性质和误差；边界、性质和集成验证 | 判断时间值的证据能力，为整个设计安排验证 |

本章覆盖普通业务系统的时间建模。日历示例采用公历，Java 是具体化工具。闰秒与不同时间尺度只交代本章结论的适用边界，不展开精密授时；跨机因果顺序、分布式等待预算和可靠执行机制交给相关后续章节。非公历规则、交易所交易日历等应明确为另外的业务日历，不能声称已穷尽所有时间领域。

## 3. 正文改写的具体要求

### 3.1 案例从属于知识结构

开篇保留会员争议，先完成有明确条件的最小推导。随后整理模型，其他案例分别用于验证某条关系。会员说明日期如何成为区间，订阅说明日历运算与锚点，日报说明区间映射与事件归属，预约说明规则生成与消歧，课程检验全部关系能否组合。

每个案例都应让读者获得可迁移的结论。删掉案例中的业务名称后，仍应能够解释机制；换一个业务后，也应能判断哪些条件要重新约定。仅将不同案例用“接下来”连接，不满足此要求。

### 3.2 定义、关系和推导形成连续解释

核心概念应说明表达什么、没有表达什么、允许什么操作，以及需要补充什么才能转换。概念表只用于回查，不能承担全部解释。

推导应包含必要中间步骤。例如“一天不总是 24 小时”要从相邻当地日界的偏移变化得到；“月底要保留锚点”要说明逐次运算怎样丢失原始日号；“统一 UTC 不够”要指出哪些业务意图没有包含在一个瞬间中。不能只陈列正确结论和错误代码。

不要求每节套用同一个显式模板，但编辑时必须能定位本节的主命题、依据、推导、应用和适用边界。若某段只能归类为“顺便提醒”，应纳入对应子问题、移到扩展部分，或删除无新增信息的重复。

### 3.3 基础、应用与扩展有明确层次

基础正文负责概念和机制，典型应用验证该机制，扩展承载产品差异、排查步骤和完整实现。影响主结论成立的前提必须留在正文，不能靠扩展链接补救。

每次引入边界情况，要说明它改变了哪个假设、怎样改变结果。连接池、历史修复、重算快照、补跑等内容应有聚合位置，不在每个段落结尾临时追加。全面性按覆盖矩阵核对，禁止为减少篇幅机械删除内容。

### 3.4 严谨性与权威性

技术事实、业务选择、实现默认行为和设计建议必须在措辞上可以区分。技术事实核对原始资料；业务选择说明承诺和代价；默认行为注明具体 API；设计建议说明适用条件。

推导使用的输入、单位、时区、端点和起算事件必须明确。避免“服务端一定准确”“UTC 可以解决全部时间问题”“日期加时区总能得到唯一瞬间”等无条件结论。类型正确、计算正确和真实业务事实可信是三个不同层次。

代码与表格帮助复核推理；引用紧邻所支持的断言，不以长串链接代替解释。未运行数据库实验的结果称为预期，不能写成实测。断言数量不能替代论证质量或读者试读。

### 3.5 语言、标题与阅读节奏

章和节的标题准确指向知识对象，子节允许使用现象或问题。使用连贯的书面汉语，减少短段堆叠、连续设问、金句与“还要注意”。定义和机制可以直接讲，不必先制造事故。

幽默来自具体工程反差，不挤占推导空间。抽象之后应有可观察结果，案例之后应有一般结论。练习集中检验理解，正文不反复让读者“先猜一下”，避免通读像一份练习讲义。

语言验收单独通读，不能以技术审校代替。重点检查抽象名词连用、缺少主语、动宾搭配生硬、翻译腔，以及过于陈腐的总结和转场。将“沿依据向派生结果传播”改成“开课时间改了，报名截止和提醒也要重算”这类读者能够直接理解的表达。术语在定义、推导和精确比较时保留，已经讲清的地方用具体对象继续解释，不在每段重复“语义、边界、依据、保证”。

每个长句检查主语、动作和结果是否清楚；需要回读才能理顺时，应拆句或重写。每个节尾检查是否真正增加归纳，删除只重复目录的“至此形成完整链条”等套话。“有人味”来自自然的观察、具体的解释和适当的节奏，不靠增加口头禅、感叹号或强行幽默。润色后重新检查否定、条件、单位和责任主体，避免语言变顺却改变技术含义。

## 4. 逐节实施与覆盖矩阵

| 节 | 主命题与必须完成的实质工作 | 必须保留的覆盖 |
| --- | --- | --- |
| 1.1—1.2 | 从承诺导出日期到瞬间的映射；解释半开区间的集合含义、相邻分割和测试依据 | 会员争议、业务时区、UTC 等价表示、起止合法性、精度与边界 |
| 1.3 | 完整解释值、量、规则的分类及转换方向；区分表示相等与瞬间相等、原始依据与派生结果 | 六类基本对象、时区与偏移量、区间、计划和事件事实 |
| 1.4 | 从时间轴与日历两种运算推导天长变化和月末非等价计算 | 23/25 小时、Duration/Period、锚点、月末策略、闰年和续期起点 |
| 1.5 | 由相邻日界映射推导查询范围，再引入事件归属与可见数据 | 半开 SQL、特殊日界、每日限额、晚到与重算入口 |
| 1.6 | 分开日程承诺、解析和生成过程；给出输入到候选再到计划的完整逻辑 | 固定瞬间/当地意图、重复规则、缺口/重叠、偏移校验、规则更新、改期 |
| 1.7 | 从语义不变量选择协议和存储策略，解释隐式转换与信息损失 | 展示、RFC 3339/epoch、列类型、会话与驱动、精度、历史数据 |
| 1.8 | 建立时间条件与状态变化的区别，沿受理、继续、清理解释判断责任 | 批任务迟到、缓存、资格记录、宽限、时钟容忍、独立保留期限 |
| 1.9 | 区分事件语义和时钟性质，推导耗时异常以及时间来源的证据范围 | 客户端/服务端、发生/接收/写入、事务时间、单调时钟、多机偏差 |
| 1.10—1.11 | 用一套需求推导模型、依赖和验证，再给出有反馈的迁移练习与知识归纳 | 课程、改期、旅行、实际结束、存取、组合验证、七天/闰日练习 |

保留现有页面地址及显式历史锚点；阅读顺序以 `SUMMARY.md` 为准。需要新增子节时保持当前十一节的主结构，避免反复改目录替代正文工作。图表编号按阅读顺序维护。扩展材料和后续章节中的已迁移内容继续保留。

## 5. 执行阶段与完成条件

以下状态按本轮实际结果更新。每阶段完成后先自查，再推进；不得用构建成功代替内容验收。

- [x] 阶段 A：建立本指引，统一规范入口，删除四份旧改造文档并清理引用。
- [x] 阶段 B：重写 1.1—1.3，完成从案例到概念体系的推导。验收：无需后文补定义，也能说明值、量、规则和转换条件。
- [x] 阶段 C：重写 1.4—1.6，按运算和解析关系组织日历、日报、预约。验收：能解释计算为什么不同，且基础原理与应用有主次。
- [x] 阶段 D：重写 1.7—1.9，建立表示、判断、来源三层保证。验收：每个建议都能对应一种信息保留或业务保证。
- [x] 阶段 E：改写综合设计与回顾，检查全章重复、覆盖和语言。验收：课程字段与测试都能回溯到前文模型，回顾能重建知识关系。
- [x] 阶段 F：核查技术依据、引用、目录、锚点、图表、构建及示例；记录实际范围。验收：工程检查通过，未验证项目明确保留。
- [x] 语言专项验收：逐页通读正文，修改拗口、陈腐、不通顺的句子，压缩重复总结；保留必要术语和技术限定，记录代表性修改。

## 6. 编辑验收问题

验收时逐节回答下面的问题，并在实施记录中说明实质改动。任一核心问题未通过，就不把该节标为完成。

1. 去掉业务故事后，这节还留下什么可复用的概念或机制？
2. 读者能否说出概念之间的关系，而不只是重复术语？
3. 关键结论能否沿正文中的中间步骤推导出来？
4. 案例和反例分别验证了哪个命题或条件？
5. 重要限制是否紧邻论证，扩展是否有明确归属？
6. 是否区分技术事实、业务约定与库默认行为？
7. 是否通过删减重复、重写解释改善主次，而非只增加文字？
8. 全章能否从头连续阅读，也能按概念回查？
9. 句子读起来是否自然，主语和动作是否清楚？是否在用抽象名词代替本可说清的具体过程？
10. 删掉套话和机械转场后，论证是否仍连贯？润色是否保留了原有条件和保证范围？

## 7. 实施记录与验证范围

本轮从用户指出“优化不彻底”开始重新验收。旧版本的构建和示例结果不能自动作为本轮完成证明。下方记录随实施更新；真实读者试读与实际数据库集成测试不得标为已完成，除非本轮确实开展。

### 7.1 本轮正文改写

2026-09-16 完成十个主题页面的实质改写，覆盖 1.1—1.11。开篇从会员承诺推导截止瞬间和半开区间，再建立值、量与规则的概念体系。日历、日报和预约分别解释运算、日界映射和候选瞬间解析；存储、生命周期和时钟分别回答如何保留含义、何时判断、判断依据从哪里来。综合课程案例重新推导字段、计算关系和验证范围，回顾增加“存取正确但判断仍错”的诊断练习。

关键推导已补入正文：用 UTC 端点解释 23 小时的一天；用原始日号的丢失解释月底锚点；用原始预约意图解释统一保存 UTC 的局限；用检查时刻与保存成功的区别解释截止前后的受理。图示与综合案例同步采用这些区分。案例仍保留，但每个案例围绕一个明确的概念或关系展开。

### 7.2 语言专项修改

逐页检查长句、抽象名词连用、动宾搭配和机械总结，修改约四十处表达。代表性修改包括：

| 原表达或问题 | 本轮处理 |
| --- | --- |
| “变更应沿依据向派生结果传播” | “开课时间改了，报名截止和提醒也要重算。” |
| 围绕“持久化派生值”“赋予权力”的抽象解释 | 直接说明提醒时刻虽然存了下来，仍由开课时间算出。 |
| “业务执行赋予边界动作含义” | “有了截止，还得说明到达截止时系统应该做什么。” |
| 节尾反复宣告“形成完整链条” | 删除重复归纳，让下一节承接尚未解决的问题。 |
| 存储、时钟和综合设计的标题过于抽象 | 改为“接口怎样表达字段含义”“时钟有误差，判断能有多准确”“哪些值要保存，哪些可以计算”等明确问题。 |

润色后复核了时区、端点、时间来源和成功条件；图中的“服务端受理”改为“资格检查通过”，并明确保存成功这一条件。此项完成表示本轮编辑通读完成，不代表经过真实读者试读，也不意味着今后不再调整文风。

### 7.3 验证结果与尚未验证的范围

- 核对 Java 时间 API、PostgreSQL 日期时间类型与函数等一手资料，引用保留在相应论述附近。
- Java 示例编译、运行通过，输出 `Checks passed: 51989`。这些检查验证确定性的时间计算，不代表课程业务系统或数据库集成已实现。
- 结构检查通过：十个主题页面、十一节主结构、八幅图和八张表的顺序一致；三十四个显式历史锚点唯一且重定向目标存在，主题页面中的三十四处本地链接和资源引用有效。
- 章节首页与 `SUMMARY.md` 阅读顺序一致，旧改造文档引用已清理。
- VitePress 构建和 `git diff --check` 通过。浏览器抽查受理章节及更新后的放大图，图中文字完整、无裁切，图文判断条件一致。
- 未开展真实读者试读、数据库与驱动的实际集成测试，也未实现综合案例中的完整课程应用。相关段落明确说明了验证范围，不将设计预期写成实测结果。
