# 第一章结构设计：从会员到期到时间建模

> 第一阶段确定的结构依据，配合 [改造清单](CH01-REVISION-PLAN.md) 使用。正文实施进度见下方说明。
>
> 实施进度：第二至第四阶段已完成，九个二级小节已落实到正文，深入材料已创建并接收内容，语言与图注已精修。下文“旧”指改造前版本，“新”指实施结构；各节目标保留作为后续验收依据。

## 一、第一次闭环：在新 1.2 结束前解决开篇错误

### 业务假设

开篇继续使用“会员有效期至 3 月 31 日”的争议。第一次解答采用以下明确约定，后续示例改变条件时需指出变化。

| 项目 | 本例选择 |
| --- | --- |
| 用户承诺 | 按上海日期计算，2027 年 3 月 31 日当天仍有效 |
| 开始边界 | `2027-03-01T00:00:00+08:00`，包含这一瞬间 |
| 失效边界 | `2027-04-01T00:00:00+08:00`，不包含这一瞬间 |
| 判断来源 | 由服务端提供本次检查使用的 `now`，读取一次；先假设时钟满足本例需要 |
| 本次动作 | 一个新请求是否满足有效时间范围；客户端点击时间不作判断依据 |
| 其他资格 | 假设会员没有暂停、撤销等其他不允许访问的情况 |
| 边界变更 | 本次判断使用确定的边界值；续期、并发更新另行引入 |

首次闭环只需要解释日期、时间点、明确时区和区间包含关系。`Asia/Shanghai` 先解释为本例采用的地区时区；`+08:00` 和 `Z` 在实际换算时说明，不要求读者先了解时区数据库。

### 解释顺序与可见结果

1. 沿着原程序走一次：`2027-03-31 00:00:00` 在 31 日刚开始就让会员失效。
2. 补全文案：31 日是最后有效日期，因此应当使用上海日期 4 月 1 日的开始作为截止。
3. 区分日期与时间点：`2027-03-31` 表示一个日期，授权还需要一个确定的截止瞬间。
4. 给出等价表示：`2027-04-01T00:00:00+08:00` 与 `2027-03-31T16:00:00Z` 指向同一瞬间。
5. 先用输入明确的简单函数表达 `starts_at <= now && now < expires_at`，再说明半开区间的名称和相邻区间的好处。
6. 给出以下检查和解释，让读者在进入接口、数据库之前知道修复是否成立。

| 服务端检查时刻（上海时间） | 满足时间范围 | 原因 |
| --- | --- | --- |
| 3 月 31 日 23:59:59.999 | 是 | 尚未到达失效边界 |
| 4 月 1 日 00:00:00.000 | 否 | 截止边界不包含在内 |
| 4 月 1 日 00:00:00.001 | 否 | 已越过截止边界 |
| 3 月 1 日 00:00:00.000 | 是 | 开始边界包含在内 |
| 2 月 28 日 23:59:59.999 | 否 | 尚未生效 |

这些时刻是便于观察的测试输入，不用于承诺数据库精度。示例函数接收 `now`；`Clock` 注入的完整实现稍后进入深入实验。区间合法性在创建时明确，不拿错误区间演示正常授权。

### 这一轮读者应当得到什么

读者能说明原程序提前一天失效的原因，能写出明确约定下的时间范围判断，并能解释为什么不用“当天最后一毫秒”表示截止。

此处解决的是时间范围判断。持久化是否保留瞬间在新 1.3 验证；真正的授权位置、持续请求和清理在新 1.4 完善；服务端时间的可信度在新 1.7 展开。第一次解答的代码名称应体现局部职责，例如 `isWithinValidityWindow`。

离开新 1.2 时留下的问题：这个截止值经过接口和数据库以后，还能保持同一含义吗？

## 二、确定的目标目录与承接

下列二级结构作为第二阶段的实施依据。表中的“内部展开顺序”描述内容次序，不要求每一项都单独成为三级标题；实际三级标题在段落形成后确定，避免预先切碎论述。

### 新 1.1：3 月 31 日到底还能不能用

- 带入问题：用户和程序为什么都觉得自己没错？
- 内部展开顺序：保留争议现场 → 指出文案缺少约定 → 区分最后有效日期与失效瞬间。
- 离开时能做：把“当天是否仍有效”“按哪里计算日期”问清楚。
- 承接：约定已明确，怎样变成一次准确的时间比较？

### 新 1.2：把最后一天变成一个边界

- 带入问题：如何兑现“上海日期 3 月 31 日当天仍有效”？
- 内部展开顺序：当地日期边界 → 偏移量与 UTC 的等价表示 → 有效条件 → 半开区间及续期衔接 → 固定输入验证。
- 离开时能做：计算本例截止值，判断开始和结束边界，完成第一次闭环。
- 承接：计算结果正确，传出去、存起来会不会变？

### 新 1.3：用户去了纽约，服务器也搬家了

- 带入问题：同一个会员的时间为什么在不同页面或环境里不同？
- 内部展开顺序：旅行只改变展示 → 显式展示时区与格式偏好 → 时间点接口 → 一个数据库往返观察 → 比较瞬间和约定精度。
- 离开时能做：区分表示变化与解释错误，确定接口字段含义，知道如何验证实际存取链。
- 承接：值没有丢失含义，程序应该在哪里使用它判断权限？

### 新 1.4：到期了，清理任务还没跑

- 带入问题：数据库有截止值，为什么任务迟到仍会延长权限？
- 内部展开顺序：新访问由授权路径判断 → 明确受理动作 → 已有操作的继续规则 → 宽限与技术容忍 → 独立的恢复和删除期限。
- 离开时能做：分别说明新请求、已受理请求和资源清理的规则；理解缓存旧判断不等于当前权限。
- 承接：固定日期的会员已能兑现，产品改成“七天”或“一个月”以后，该怎么计算？

### 新 1.5：七天、一个月，究竟有多长

- 带入问题：购买一段有效期，能否统一换算成秒？
- 内部展开顺序：固定时长与日历日 → 用春季跳时解释自然日长度变化 → 月末锚点与两种连续计算结果 → 年度规则的短练习 → 业务日查询作为区间迁移。
- 离开时能做：依据承诺选择长度或日历计算，保留原始锚点，独立求出业务日的两个边界。
- 承接：知道日历规则如何产生边界后，“每天当地九点”如何生成每次计划？

### 新 1.6：每天九点，下一次是哪一刻

- 带入问题：为什么保存今天九点的 UTC 值不能完整表示未来营业安排？
- 内部展开顺序：时刻值与重复规则 → 地区时区与固定偏移量的区别 → 未来承诺固定瞬间还是当地时刻 → 缺口与重叠 → 一种明确的预约策略及输入协议。
- 离开时能做：保留计划意图，识别零个、一个或两个候选瞬间，说明本例如何消歧；知道规则改变可能要求重新生成计划。
- 承接：时间值和规则都明确了，比较使用的“现在”从哪里来？

### 新 1.7：时间存对了，“现在”可靠吗

- 带入问题：手机调回去年能否续命，请求耗时为什么可能为负？
- 内部展开顺序：客户端与服务端的职责 → 事件时间与接收时间 → 钟表回拨对耗时的影响 → 单调计时与生命周期 → 多机时间的限制。
- 离开时能做：选择记录和计时的来源，不把本地单调读数用于跨进程持久化，不把跨机时间戳当作充分的因果证据。
- 承接：换成一场跨时区课程，能否自己完成这些选择？

### 新 1.8：换一个需求，自己设计一次

- 带入问题：面对新需求，能否自己发现模糊约定？
- 内部展开顺序：课程原始需求与短暂停顿 → 参考业务选择 → 数据与派生关系 → 改期、旅行、实际结束延后的判断 → 组合验证与带反馈的练习。
- 离开时能做：区分编辑意图、发布计划、实际事实和派生数据，并为改变条件后的结果写出检查。
- 承接：回到开篇，用简洁规则向用户和工程师解释同一个结果。

### 新 1.9：本章回顾

- 带入问题：以后看到一个时间字段，应该按什么顺序想？
- 内部展开顺序：回到会员承诺 → 串起规则、数据、判断与验证 → 说明基础方案与额外设计的适用条件 → 衔接下一章的 ID。
- 离开时能做：重复使用“明确含义 → 定义边界与来源 → 保留语义 → 验证承诺”的判断顺序。
- 本节不引入新技术或新的边界情况。

## 三、深入材料的具体落点

主线中保留能作正确选择的必要前提，深入材料承接完整代码、排查步骤和可靠性设计。以下路径已在第二阶段创建并接收相关材料。扩展材料继续随正文精修，部分性质测试在正文保留摘要以供组合验证。

统一收纳到 `examples/ch01-time/deep-dive.md`，在现有示例 README 增加导航，并从相应主线小节链接到对应主题。材料建立时使用下表显式锚点。

| 编号 / 锚点 | 主题 | 接收内容与主线入口 |
| --- | --- | --- |
| E1 / `storage-roundtrip` | 一个时间值如何走完存取链 | 主线 1.3；产品类型对照、完整 SQL、驱动与连接池、精度、数值范围、严格解析、排序和导出陷阱 |
| E2 / `local-time-resolution` | 从当地输入得到唯一瞬间 | 主线 1.5、1.6；完整消歧函数、候选校验、特殊日界、规则变化、日历与解析性质测试 |
| E3 / `historical-time-repair` | 旧时间数据如何查证和修复 | 主线 1.3；独立流水核对、混合来源、保留原值与推定依据；迁移实施衔接第 23 章 |
| E4 / `business-day-jobs` | 日报迟到、重算与计划执行 | 主线 1.5、1.6；晚到数据、报表快照、派生日期、调度模式、补跑、计划身份与物化、规则过渡、离线事件练习及反馈 |
| E5 / `validity-and-clock-tests` | 时间范围的实现与验证边界 | 主线 1.4、1.7、1.8；`Clock` 完整示例、缓存与撤销更新、不可逆失效状态、客户端倒计时估计、设备校正、生产集成验证边界 |

另外两项材料进入已有后续章节文件：

- 第 9 章 `manuscript/part-03-repetition/09-timeouts.md`：本地共同等待预算、零值超时参数、取消与远端执行、跨机器预算传播。接收工作包括原解释、伪代码和共同预算图；在该章成稿前以明确标注的待编排素材保存。
- 第 21 章 `manuscript/part-05-distribution/21-distributed-time.md`：多机误差算例、误差范围内的先后判断、集中判定的收益与代价。第一章保留短例与限制，详细论证随材料接收。

搬移约束：先让接收材料包含完整解释和必要前提，再从原位置压缩或移除。只增加一句未来引用不算完成搬移；不把尚未成稿章节的未有内容描述为已展开。

## 四、现有内容逐节去向

这是一份搬移账本。覆盖原 32 个三级小节，并单独处理开篇、目录与回顾；一节拆分到多个位置时，明确各自接收的内容。

### 旧 1.1：时间含义

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.1.1 时间点、持续时间与日历值 | 拆分主线：日期与瞬间到新 1.1—1.2；持续时间到新 1.5；当地值、规则与 `Local` 到新 1.6；来源限制到新 1.7 | 生日不补午夜；类型按需要引入；值与规则分开 |
| 1.1.2 到期日与有效区间 | 主线新 1.2 | 排他边界、相邻区间、业务承诺优先 |
| 1.1.3 受理边界、宽限期与时钟容忍 | 合并到主线新 1.4 | 点击、受理、提交不同；宽限与技术误差不同 |

### 旧 1.2：时区与日历

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.2.1 UTC、偏移量与地区时区 | 简单换算到新 1.2；二次转换与接口歧义到新 1.3；地区规则到新 1.6 | 相同瞬间可以有不同表示；偏移量不等于地区规则 |
| 1.2.2 展示时区不是服务器时区 | 主线新 1.3 | 展示参数明确；语言、国家不能直接替代时区选择 |
| 1.2.3 未来日程该保存什么 | 主线新 1.6；规则更新的操作细节到 E2、E4 | 固定瞬间与保持当地时刻是不同承诺 |
| 1.2.4 不存在与重复的当地时间 | 新 1.5 先讲春季跳时与一天长度；新 1.6 讲输入缺口和秋季重叠 | 前者解释日历算术，后者解释解析选择，避免重复整段讲解 |
| 1.2.5 预约时间的解析与消歧 | 主线新 1.6 保留候选逻辑与协议配合；完整 Java 到 E2 | 选一种明确策略，保留偏移量相容性检查 |
| 1.2.6 月度账期与日历锚点 | 主线新 1.5；连续性质测试到 E2 | 连续加月与原始锚点对照、月末模型、年度规则、续期起算选择 |

### 旧 1.3：接口与存储

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.3.1 Unix 时间戳、单位与数值精度 | 新 1.3 压缩保留起点、单位、精度约定和缺失语义；安全整数、范围、闰秒限制到 E1 | 数字也需要协议；表示精度不等于时钟准确度，后者衔接新 1.7 |
| 1.3.2 时间接口的协议约定 | 时间点协议与语义往返到新 1.3；预约输入到新 1.6；完整严格解析、排序、编辑丢精度、CSV 到 E1 | 不靠服务器默认解释，不直接追加 `Z`，区分展示与保存 |
| 1.3.3 数据库类型与会话时区 | 新 1.3 以一个 PostgreSQL 往返观察串起结果；完整 SQL 与产品对照到 E1；记录时机到新 1.7 | 数据库类型未必保存地区时区；保留输入前提，示例不承诺提交时刻 |
| 1.3.4 驱动、连接池与精度往返 | 新 1.3 保留转换链与验证目标；实现细节到 E1；事件时间与行更新时间到新 1.7 | 比较实际链路的语义，不以客户端显示代替验证 |
| 1.3.5 历史时间数据的修复 | 深入材料 E3，新 1.3 末给入口 | 先定位错误层，来源丢失不能靠平移恢复 |

### 旧 1.4：时钟

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.4.1 钟表时间与单调计时 | 主线新 1.7，共同预算部分归第 9 章 | 负耗时对照、同一时钟差值、持久化与重启边界 |
| 1.4.2 等待预算与截止时间的生命周期 | 预算实现归第 9 章；生命周期及为何反复读钟在新 1.7 简述 | 共用预算而非每步重置，等待结束不保证远端停止 |
| 1.4.3 客户端与设备时间 | 新 1.7 保留最终判断方和发生/接收时间；估计与校正细节到 E5；结账应用到 E4 | 不信任客户端授权时间，也不覆盖有价值的设备原始时间 |
| 1.4.4 多机时钟与判定权威 | 新 1.7 压缩保留限制；完整误差与集中判定论证归第 21 章 | 服务端可控不等于绝对准确 |

### 旧 1.5：权限

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.5.1 权限失效与后台清理 | 主线新 1.4 | 清理迟到不延长权限，缓存的是过去判断 |
| 1.5.2 持续操作、撤销与多种期限 | 主线新 1.4 合并继续、撤销区别与清理期限；缓存更新及不可逆终态细节到 E5 | 时间判断只是授权条件之一，不暗示已解决数据更新或撤销传播 |

### 旧 1.6：业务日与任务

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.6.1 业务日、日界与查询范围 | 主线新 1.5；特殊午夜的完整处理到 E2 | 独立求两个日界、半开查询、日限额不随用户旅行重置 |
| 1.6.2 事件归属、晚到数据与重算 | 深入材料 E4；新 1.5 简述采用哪个事件时间，并明确区间固定不保证输入固定 | 收集截止、快照、修订与结账是额外选择 |
| 1.6.3 查询方式与派生业务日期 | 新 1.5 保留范围查询；表达式索引与派生日期演进到 E4 | 时区口径与查询性能分别验证，跨地区日期相加有口径区别 |
| 1.6.4 固定速率、固定延迟与日历计划 | 新 1.6 对比日历计划与固定间隔；三种调度模式及调度器行为到 E4 | 生成目标时刻不等于保证执行 |
| 1.6.5 工作身份、漏执行与规则变化 | 深入材料 E4；新 1.6 留一段说明显式计划输入与执行分工 | 业务日期、补跑身份、错过策略、规则过渡与物化成本 |

### 旧 1.7：综合案例

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.7.1 输入意图与业务时间约定 | 重写为新 1.8 的需求、读者判断与参考选择 | “30 天”选择 720 小时必须显式说明，实际结束不能由计划替代 |
| 1.7.2 数据模型与接口协议 | 新 1.8 参考设计 | 原始意图、发布瞬间、实际事件和派生字段分别负责什么 |
| 1.7.3 改期、任务执行与权限验证 | 新 1.8 保留哪些值应变/不变与权限判断；队列版本和迟到执行细节到 E4 | 主线不依赖版本号实现；E4 保留单次版本检查不保证并发原子性的限定 |

### 旧 1.8：验证与练习

| 原小节 | 处理与目标 | 保留重点 |
| --- | --- | --- |
| 1.8.1 固定时钟与边界测试 | 固定 `now` 输入和结果前移到新 1.2；`Clock` 完整代码到 E5；新 1.8 作组合验证 | 无需真实等待，开始边界和非法区间也要处理 |
| 1.8.2 日历性质、时区与消歧测试 | 局部检查分别跟随新 1.3、1.5、1.6；完整性质验证到 E2；新 1.8 汇合 | 默认环境变化不改变语义；修改全局时区有隔离前提 |
| 1.8.3 接口、数据库与调度测试 | 新 1.3、1.8 保留测试分层；数据库细节到 E1；调度测试到 E4 | 确定性示例不等于驱动、调度和运行环境保证 |
| 1.8.4 设计练习 | 七天、闰日练习在新 1.5、1.8 组织，不重复全文；离线结账题到 E4 | 都需参考判断路径，允许不同承诺但不能遗漏决定因素 |

开篇争议保留；开篇目录随正文最终结构同步，暂不提前发布新标题。旧 1.9 重写为新 1.9，保留会员规则闭环、适用范围判断与下一章入口。

## 五、知识依赖检查

| 使用位置 | 需要先理解 | 结构处理结果 |
| --- | --- | --- |
| 新 1.2 的 UTC 值与比较 | 日期和瞬间、明确时区、偏移量记法 | 在新 1.1—1.2 内完成，不等待新 1.6 |
| 新 1.3 的存取实验 | 同一瞬间有不同表示、截止值含义 | 已由新 1.2 建立；实验先给结果，不以驱动细节作为理解前提 |
| 新 1.4 的受理判断 | 时间范围、服务端 `now` 的明确假设 | 已在首次闭环声明；此处补业务动作，时钟限制在新 1.7 回访 |
| 新 1.5 的自然日计算 | 明确时区、区间、时长与日历日区别 | 本节先讲跳时现象再算日界，不先使用新 1.6 的解析算法 |
| 新 1.6 的预约输入 | 当地值、地区规则、候选瞬间 | 协议紧跟本节策略，不放在新 1.3 提前使用 |
| 新 1.8 的课程案例 | 日历/时长、输入解析、时间来源、授权规则 | 前文均已出现；版本执行细节进入 E4，报表只作基本业务日应用 |
| 新 1.8 的练习反馈 | 练习涉及的概念已学过 | 离线结账题跟随 E4；主线保留已具备前提的练习 |

术语首次出现的分配：新 1.1—1.2 讲日期、时间点、时区的本例用法、UTC、偏移量、半开区间；新 1.3 讲数值时间戳和往返；新 1.4 讲受理边界；新 1.5 讲持续时间、日历量和锚点；新 1.6 讲地区规则、缺口、重叠与消歧；新 1.7 讲单调计时和来源误差。Java 类型在对应概念出现后引入。

重复控制：半开区间先在会员里学会，再用于日报；夏令时先解释日历长度，再解释输入歧义；测试先作局部反馈，再作综合验证。每次回访要增加一个用途，不重复整套定义。

## 六、第一阶段验收与后续实施边界

- [x] 第一次闭环位置确定为新 1.2 结束前，已有业务假设、规则与可观察结果。
- [x] 原 32 个三级小节及开篇、回顾均有去向；外移内容已分配具体文件与主题。
- [x] 九个二级小节都有进入问题、展开顺序、学习结果与承接。
- [x] 检查并调整自然日与夏令时、预约协议与解析、课程案例与版本机制的依赖。
- [x] 主线明确简单方案的适用范围，深入材料仍保留防止误用所需的前提。

上述勾选记录第一阶段的结构验收。第二阶段已按此依据完成会员案例和结构搬移，第三阶段已补学习反馈；实际目录与锚点已同步。第四阶段已完成语言、图注与示例尺度的统一检查。下一步为第五阶段整章验收和页面视觉检查，真实读者试读仍未验证。
